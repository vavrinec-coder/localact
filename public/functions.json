{
  "functions": [
    {
      "id": "DIAG",
      "name": "DIAG",
      "description": "Diagnostic function that confirms the LocalAct custom function runtime can execute JavaScript.",
      "parameters": [],
      "result": { "type": "number", "dimensionality": "scalar" }
    },
    {
      "id": "DIAG_BACKEND",
      "name": "DIAG_BACKEND",
      "description": "Diagnostic function that confirms the LocalAct custom function runtime can reach the local backend.",
      "parameters": [],
      "result": { "type": "number", "dimensionality": "scalar" }
    },
    {
      "id": "DIAG_DATE",
      "name": "DIAG_DATE",
      "description": "Diagnostic function that returns the exact date LocalAct will send to the backend.",
      "parameters": [
        { "name": "date", "description": "Exact Excel date.", "type": "any", "dimensionality": "scalar" }
      ],
      "result": { "type": "string", "dimensionality": "scalar" }
    },
    {
      "id": "LOAD_PL",
      "name": "LOAD_PL",
      "description": "Returns the summed P&L amount for one summary account and exact date.",
      "parameters": [
        { "name": "summary_account", "description": "Summary Account.", "type": "string", "dimensionality": "scalar" },
        { "name": "date", "description": "Exact Excel date.", "type": "any", "dimensionality": "scalar" }
      ],
      "result": { "type": "number", "dimensionality": "scalar" }
    },
    {
      "id": "LOAD_BS",
      "name": "LOAD_BS",
      "description": "Returns the summed Balance Sheet amount for one summary account and exact date.",
      "parameters": [
        { "name": "summary_account", "description": "Summary Account.", "type": "string", "dimensionality": "scalar" },
        { "name": "date", "description": "Exact Excel date.", "type": "any", "dimensionality": "scalar" }
      ],
      "result": { "type": "number", "dimensionality": "scalar" }
    },
    {
      "id": "LOAD_PL_DEPT",
      "name": "LOAD_PL_DEPT",
      "description": "Returns the summed P&L amount for one summary account, exact date, and summary department.",
      "parameters": [
        { "name": "summary_account", "description": "Summary Account.", "type": "string", "dimensionality": "scalar" },
        { "name": "date", "description": "Exact Excel date.", "type": "any", "dimensionality": "scalar" },
        { "name": "summary_department", "description": "Summary Department.", "type": "string", "dimensionality": "scalar" }
      ],
      "result": { "type": "number", "dimensionality": "scalar" }
    },
    {
      "id": "LOAD_OPS",
      "name": "LOAD_OPS",
      "description": "Returns the summed operations amount for one metric label, exact date, and raw channel.",
      "parameters": [
        { "name": "metric_label", "description": "Metric label.", "type": "string", "dimensionality": "scalar" },
        { "name": "date", "description": "Exact Excel date.", "type": "any", "dimensionality": "scalar" },
        { "name": "channel", "description": "Raw Channel.", "type": "string", "dimensionality": "scalar" }
      ],
      "result": { "type": "number", "dimensionality": "scalar" }
    }
  ],
  "allowCustomDataForDataTypeAny": true
}
